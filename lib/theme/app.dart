import 'package:flutter/widgets.dart';

class Shadow {
  static const sm = BoxShadow(
    color: Color.fromRGBO(149, 157, 165, 0.1),
    blurRadius: 14,
    spreadRadius: 0,
    offset: Offset(
      0,
      8,
    ),
  );
}
