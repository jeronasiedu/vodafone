import 'package:flutter/material.dart';

class ValueAddedServicesPage extends StatelessWidget {
  const ValueAddedServicesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
