import 'package:flutter/material.dart';

class FixedBroadbandPage extends StatelessWidget {
  const FixedBroadbandPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
